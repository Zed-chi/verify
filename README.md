# Verify - "Скобко проверятель"
***

## Установка

Пример cmd:

```bash
   composer require zedchi/verify
```
## Использование:

```php
    $b = new Zedchi\Verify();

    $b->verify(СКОБОЧНАЯ КОНСТРУКЦИЯ);
```
